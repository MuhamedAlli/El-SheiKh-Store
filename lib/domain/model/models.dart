//onBoarding Model
class SliderObject {
  String title;
  String subTitle;
  String image;
  SliderObject(this.title, this.subTitle, this.image);
}

//Login models
class LoginModel {
  String token;
  LoginModel(this.token);
}
